

export function signinUser({ email, password }) {
    // Submin email and password to server

    // If request is good
    // - Update state to indicate user in authenticated
    // - Save the JWT token
    // - redirect to the route '/feature'
}